<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Allow access to files in cache directory -->
    <cache-path
        name="cache"
        path="." />

    <!-- Optional: allow access to external files if needed -->
    <external-path
        name="external"
        path="." />

    <external-files-path
        name="external_files"
        path="." />
</paths>